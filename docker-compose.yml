# Por defecto: desarrollo con hot reload (Vite HMR + tsx watch)
# App: http://localhost:5173  |  API: http://localhost:4000
#
# Producción local (sin HMR): docker compose -f docker-compose.prod.yml up --build

include:
  - path: docker-compose.dev.yml
